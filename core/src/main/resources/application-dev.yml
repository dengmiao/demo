spring:
  # 数据源
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/corgi?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=UTC
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  # redis
  redis:
    host: 127.0.0.1
    database: 2
    port: 6379
    timeout: 3000